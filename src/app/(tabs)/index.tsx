import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function WorkoutScreen() {
  return (
    <ScreenPlaceholder
      title="Entreno"
      description="Aqui se empieza una sesion vacia o desde una rutina. Llega en la fase 1."
    />
  );
}
