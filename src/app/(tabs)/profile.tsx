import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function ProfileScreen() {
  return (
    <ScreenPlaceholder
      title="Perfil"
      description="Peso corporal, objetivos, unidades y exportar datos. Llega en la fase 4."
    />
  );
}
